import * as workers from '../services/workerService.js'
import { validateWorker } from '../validation/workerValidation.js'
export async function list(request,response,next){try{response.json(await workers.listWorkers(request.query))}catch(error){next(error)}}
export async function get(request,response,next){try{const worker=await workers.getWorker(request.params.id);if(!worker)return response.status(404).json({message:'Worker not found.'});response.json(worker)}catch(error){next(error)}}
export async function create(request,response,next){try{const error=validateWorker(request.body);if(error)return response.status(400).json({message:error});const duplicate=await workers.ensureNoDuplicate(request.body);if(duplicate)return response.status(409).json({message:'A matching Aadhaar, mobile number, or worker card already exists.',duplicate});response.status(201).json(await workers.createWorker(request.body,request.user.sub))}catch(error){next(error)}}
export async function update(request,response,next){try{if(!await workers.getWorker(request.params.id))return response.status(404).json({message:'Worker not found.'});const error=validateWorker(request.body);if(error)return response.status(400).json({message:error});const duplicate=await workers.ensureNoDuplicate(request.body,request.params.id);if(duplicate)return response.status(409).json({message:'Duplicate worker information.',duplicate});response.json(await workers.updateWorker(request.params.id,request.body,request.user.sub))}catch(error){next(error)}}
export async function destroy(request,response,next){try{const [result]=await workers.removeWorker(request.params.id);if(!result.affectedRows)return response.status(404).json({message:'Worker not found.'});response.status(204).end()}catch(error){next(error)}}
export async function stats(_request,response,next){try{response.json(await workers.dashboardStats())}catch(error){next(error)}}

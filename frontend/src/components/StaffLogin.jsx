import { useState } from 'react'
import { Eye, EyeOff, LockKeyhole, ShieldCheck } from 'lucide-react'
import { useAuth } from '../context/AuthContext'
import './StaffLogin.css'
export default function StaffLogin({onBack,onSuccess}){const{login,expired,clearExpired}=useAuth();const[id,setId]=useState('');const[password,setPassword]=useState('');const[show,setShow]=useState(false);const[loading,setLoading]=useState(false);const[error,setError]=useState('');const submit=async(e)=>{e.preventDefault();if(!id||!password)return setError('कृपया username/email आणि password भरा.');setLoading(true);setError('');try{await login(id,password);clearExpired();onSuccess()}catch(err){setError(err.status===401?'Username/email किंवा password चुकीचा आहे.':'Login करता आला नाही. कृपया पुन्हा प्रयत्न करा.')}finally{setLoading(false)}};return <main className="login-page"><section><button className="back" onClick={onBack}>← Back to website</button><div className="login-card"><span className="login-seal"><ShieldCheck/></span><p>STAFF ACCESS</p><h1>Ratnagiri District General Workers Union</h1><small>रत्नागिरी जिल्हा जनरल कामगार युनियन (संलग्न सीटू)</small>{expired&&<div className="login-error">Your session has expired. Please log in again.</div>}{error&&<div className="login-error">{error}</div>}<form onSubmit={submit}><label>Username or email<input autoComplete="username" value={id} onChange={e=>setId(e.target.value)} /></label><label>Password<div className="password-input"><input autoComplete="current-password" type={show?'text':'password'} value={password} onChange={e=>setPassword(e.target.value)}/><button type="button" onClick={()=>setShow(!show)}>{show?<EyeOff/>:<Eye/>}</button></div></label><button disabled={loading} className="login-submit">{loading?'Signing in…':<><LockKeyhole/> Sign in securely</>}</button></form><span className="login-note">For authorized Admin, Operator and Viewer staff only.</span></div></section></main>}
